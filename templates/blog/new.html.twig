{% extends 'base.html.twig' %}

{% block title %}Add new Article{% endblock %}

{% block body %}
    <div class="page-header">
        <h3 class="page-title">
            {% if form.vars.value.id is not null %}
                Update article : <span class="text-primary">{{ article.title }}</span>
            {% else %}
                Add new article
            {% endif %}
        </h3>
    </div>

    {{ include('blog/_form.html.twig') }}

{% endblock %}
