<div class="container-fluid main-navbar" style="border-bottom: 0.0625rem solid #E4E4E4; position:fixed; top: 0;">
    <nav class="navbar navbar-expand-lg navbar-light">
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarTogglerDemo01"
                aria-controls="navbarTogglerDemo01" aria-expanded="false" aria-label="Toggle navigation">
            <img src="{{ asset('build/front/navbar-toggler.svg') }}" alt="navbar toggler">
        </button>
        <a class="navbar-brand d-block d-lg-none" href="{{ path('front_index') }}">
            <img class="img-fluid" src="{{ asset('build/dashboard/logo1.png') }}" alt="alwarda icon">
        </a>
        <a class="notifBtn navbar-round-btn d-block d-lg-none get-notification" href="#">
            <span class="notifications notifications-mobile d-none">1</span>
            <div class="navbar-btn-bg"></div>
            <img src="{{ asset('build/front/cart.svg') }}" alt="cart icon">
        </a>
        <div class="collapse navbar-collapse" id="navbarTogglerDemo01">
            <ul class="navbar-nav  mt-2 mt-lg-0">
                <li class="nav-item">
                    <a class="nav-link" href="#about">{{ 'About'|trans }}</a>
                </li>
                <li class="nav-item dropdown" style="width: 72px;">
                    <a class="nav-link custom-dropdown" href="#"
                       role="button"
                       id="navbarDropdownCatalog"
                       data-toggle="dropdown"
                       aria-haspopup="true"
                       aria-expanded="false"> {{ 'Catalog'|trans }}</a>
                    <div class="categories-dropdown dropdown-menu" aria-labelledby="navbarDropdownCatalog">
                        {# Categories go here via JS #}
                    </div>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#delivery">{{ 'Delivery & payment'|trans }}</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#footer">{{ 'Contacts'|trans }}</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('front_blog_list') }}">{{ 'Blog'|trans }}</a>
                </li>
            </ul>
            <a id="main-navbar-logo" class="navbar-brand d-none d-lg-block m-auto" href="{{ path('front_index') }}">
                <img class="img-fluid" src="{{ asset('build/dashboard/logo1.png') }}" alt="">
            </a>
            <ul class="navbar-nav  mt-2 mt-lg-0 navbar-right">
                <li class="nav-item d-flex flex-column">
                    <label for="navbarDeliverySelect" class="m0">{{ 'Delivery in'|trans }}</label>
                    <div class="d-flex">
                        <div id="navbar-map-pin">
                            <img src="{{ asset('build/front/map-pin.svg') }}" alt="map pin">
                        </div>
                        <div class="dropdown" id="navbarDeliverySelect" style="padding-top: 0.2rem;">
                            {# dropdown label goes here via JS #}
                            <div class="dropdown-menu" aria-labelledby="dropdownMenuLink">
                                {# dropdown elements gors here via JS #}
                            </div>
                        </div>
                    </div>
                </li>
                <li class="nav-item pt-3">
                    <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
                        {#                        <li class="nav-item navbar-btn-link">#}
                        {#                            <a class="d-none d-lg-block " href="#">#}
                        {#                                <img src="{{ asset('build/front/heart.svg') }}" alt="heart ison">#}
                        {#                            </a>#}
                        {#                        </li>#}
                        <li class="nav-item navbar-btn-link">
                            <a class="d-block navbar-login-btn" data-loggedin="{{ app.user is not null }}"
                               href="{{ path('front_profile') }}">
                                <div class="navbar-btn-bg"></div>
                                <img src="{{ asset('build/front/user.svg') }}" alt="cart icon">
                            </a>
                        </li>
                        <li class="nav-item navbar-btn-link">
                            <a class="notifBtn d-none d-lg-block" href="#">
                                <span class="notifications d-none">1</span>
                                <div class="navbar-btn-bg"></div>
                                <img src="{{ asset('build/front/cart.svg') }}" alt="cart icon">
                            </a>
                        </li>
                        <li class="nav-item dropdown" style="width: 72px;">
                            <a class="nav-link custom-dropdown" href="#"
                               role="button"
                               id="navbarDropdownLocale"
                               data-toggle="dropdown"
                               aria-haspopup="true"
                               aria-expanded="false">
                                {% if app.request.locale == "en" %}
                                    English
                                {% else %}
                                    العربية
                                {% endif %}
                            </a>
                            <div class="locales-dropdown dropdown-menu" aria-labelledby="navbarDropdownCatalog">
                                <a class="dropdown-item" href="{{ path('front_index', {_locale:'en'}) }}">
                                    English</a>
                                <a class="dropdown-item" href="{{ path('front_index', {_locale:'ar'}) }}">
                                    العربية</a>
                            </div>
                        </li>
                    </ul>
                </li>
            </ul>
        </div>
    </nav>
</div>
{% if app.request.locale == 'ar' %}
    <style>
        .main-navbar {
            direction: rtl;
        }
        .dropdown-menu .dropdown-item {
            text-align: right;
        }
        .nav-item>label {
            text-align: right;
        }
        #main-navbar-logo {
            width: 33%;
        }
    </style>
{% endif %}

{% include 'front/_modalCheckOut.html.twig' %}
{% include 'front/_modalTrackOrder.html.twig' %}
{% include 'front/_modalAuth.html.twig' %}
